# UserWebPart
A simple SharePoint Framework (SPFx) webpart that displays the current user's first name using the site's title font family from the SharePoint theme, automatically updating when the theme changes, and allowing customizable font size configuration through the property pane.
